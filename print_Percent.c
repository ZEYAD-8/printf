#include "main.h"
/**
 * printPercent - Prints the percent symbol
 * @args: list of arguments
 * Return: the amount of characters printed - 1.
 */
int printPercent(attribute((unused)) va_list args)
{
return (_putchar('%'));
}
